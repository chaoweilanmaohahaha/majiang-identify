# 任务清单：听牌判定前端界面

## 1. 页面骨架与视觉主题

- [x] 1.1 改造 `src/pages/index/index.vue`：搭建牌桌风格页面骨架（标题、桌面区、选牌区、结果区、按钮），验证 `npm run build:mp-weixin` 构建成功且页面渲染
- [x] 1.2 实现绿色呢绒桌面背景、白底牌面等 CSS 主题样式（rpx 单位），验证微信开发者工具中页面显示正常

## 2. 手牌选择交互

- [x] 2.1 实现 31 种牌（万/条/筒/风）分组选择器与已选数量角标，验证点选与数量上限（每种 ≤4、总数 ≤13）生效
- [x] 2.2 实现已选手牌在桌面区展示、点击移除与清空操作，验证操作后已选状态正确

## 3. 判定与结果展示

- [x] 3.1 实现"听牌判定"按钮（仅 13 张时可用）并接入 `getListeningTiles`，验证听牌手牌得到正确听口
- [x] 3.2 实现结果区：听牌展示听口牌面、未听牌展示提示，验证两种结果展示正确
- [x] 3.3 已选牌变化后结果更新逻辑，验证增删牌后重新判定结果正确

## 4. 收尾验证

- [x] 4.1 运行 `npm run type-check` 确认无类型错误
- [x] 4.2 运行 `npm run build:mp-weixin` 确认构建成功，并在微信开发者工具中手工验证交互（选牌/移除/清空/判定/结果展示）
- [x] 4.3 对照 spec.md 逐条复查行为一致，`npx openspec validate add-tingpai-ui --strict` 通过
